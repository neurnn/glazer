using System;
using System.Threading.Tasks;

namespace cligl
{
    class Program
    {
        /// <summary>
        /// Cligl (CLI utility for GLazer).
        /// </summary>
        /// <param name="Args"></param>
        static async Task<int> Main(string[] Args)
        {

            return 0;
        }
    }
}
